{
  "name": "@nails/root",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "inst": "npm install --package-lock-only --no-package-lock --prefix",
    "postinst": "npm install"
  },
  "devDependencies": {
    "wsrun": "^5.2.4"
  }
}
